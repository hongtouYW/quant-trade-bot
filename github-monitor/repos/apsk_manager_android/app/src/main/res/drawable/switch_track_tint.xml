<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ON state -->
    <item android:state_checked="true">
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/gray_979797" />
            <solid android:color="@color/green_00FF26" /> <!-- iOS green -->
            <corners android:radius="13dp" />
        </shape>
    </item>

    <!-- OFF state -->
    <item>
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/gray_979797" />
            <solid android:color="@android:color/transparent" /> <!-- iOS light gray -->
            <corners android:radius="13dp" />
        </shape>
    </item>
</selector>
