#!/usr/bin/expect -f

set timeout 30
set password "qweQWE123!@#abc"
set host "139.162.41.38"

spawn ssh -o StrictHostKeyChecking=no root@$host

expect {
    "*password:" {
        send "$password\r"
        exp_continue
    }
    "*#" {
    }
}

send "curl -s http://localhost:5001/api/stats | python3 -m json.tool | grep -E 'initial_capital|total_pnl|current_capital|margin_used|available_capital|balance'\r"
expect "*#"

send "exit\r"
expect eof
