#!/usr/bin/expect -f

set timeout 30
set password "qweQWE123!@#abc"
set host "139.162.41.38"
set file "quant-trade-bot/index.html"

# 上传index.html文件
spawn scp -o StrictHostKeyChecking=no $file root@$host:/opt/trading-bot/quant-trade-bot/

expect {
    "*password:" {
        send "$password\r"
        exp_continue
    }
    "Permission denied" {
        puts "\n❌ 上传失败"
        exit 1
    }
    eof {
        puts "\n✅ index.html上传成功"
    }
}
