# 🎯 实盘模拟交易系统完整指南

## 📊 系统概览

**实盘模拟交易监控**是一个完全自动化的加密货币交易系统，使用多时间框架技术分析策略，实时监控15个主流币种，自动执行买卖信号。

### 核心特性

- ✅ **15个监控币种**：主流币、DeFi、Layer1全覆盖
- ✅ **3倍杠杆交易**：放大收益，严格风控
- ✅ **完整数据库记录**：所有买卖操作100%保存
- ✅ **Telegram实时通知**：开仓、平仓、盈亏即时推送
- ✅ **自动止损止盈**：30秒检查一次，保护利润
- ✅ **多策略信号**：MA/RSI/MACD/布林带综合判断

---

## 💰 监控币种列表

### 主流币 (5个)
- **BTC/USDT** - 比特币：加密货币龙头
- **ETH/USDT** - 以太坊：智能合约平台
- **BNB/USDT** - 币安币：交易所代币
- **XMR/USDT** - 门罗币：隐私币龙头
- **SOL/USDT** - Solana：高性能公链

### DeFi & Layer1 (5个)
- **ADA/USDT** - Cardano：学术派公链
- **DOT/USDT** - Polkadot：跨链协议
- **AVAX/USDT** - Avalanche：子网生态
- **MATIC/USDT** - Polygon：以太坊Layer2
- **LINK/USDT** - Chainlink：预言机龙头

### 潜力币 (5个)
- **ATOM/USDT** - Cosmos：跨链枢纽
- **NEAR/USDT** - Near Protocol：分片技术
- **FTM/USDT** - Fantom：高性能DeFi
- **ALGO/USDT** - Algorand：纯PoS共识
- **VET/USDT** - VeChain：供应链溯源

---

## 🔧 交易参数配置

### 资金管理
```python
初始资金: $1,000 USDT
杠杆倍数: 3x
单笔风险: 2% ($20)
最大仓位: 30% ($300)
```

### 风控设置
```python
止损: 3% (触发自动平仓)
止盈: 6% (触发自动平仓)
风险收益比: 最低 1:2 (不满足不开仓)
滑点: 0.05%
手续费: 0.1% (Maker/Taker)
```

### 扫描周期
```python
市场扫描: 每5分钟 (300秒)
止损检查: 每30秒
每日报表: 每天00:00自动生成
```

---

## 📦 数据库结构

### 1. 交易表 (trades)
**记录所有买卖操作**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增主键 |
| timestamp | TEXT | 交易时间 |
| symbol | TEXT | 交易对 |
| side | TEXT | buy/sell |
| price | REAL | 成交价格 |
| quantity | REAL | 数量 |
| leverage | INTEGER | 杠杆倍数 |
| cost | REAL | 成本/价值 |
| fee | REAL | 手续费 |
| pnl | REAL | 盈亏金额 |
| pnl_pct | REAL | 盈亏百分比 |
| reason | TEXT | 交易原因 |
| balance_after | REAL | 交易后余额 |

**✅ 保证：** 每笔买入和卖出都会立即写入数据库，带有完整验证日志。

### 2. 持仓表 (positions)
**记录当前和历史持仓**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增主键 |
| symbol | TEXT | 交易对（唯一） |
| quantity | REAL | 持仓数量 |
| entry_price | REAL | 入场价格 |
| entry_time | TEXT | 建仓时间 |
| leverage | INTEGER | 杠杆倍数 |
| stop_loss | REAL | 止损价格 |
| take_profit | REAL | 止盈价格 |
| cost | REAL | 保证金成本 |
| status | TEXT | open/closed |

**✅ 保证：** 开仓时写入，平仓时更新状态为closed，永久保留记录。

### 3. 统计表 (stats)
**记录系统性能指标**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 自增主键 |
| timestamp | TEXT | 统计时间 |
| balance | REAL | 当前余额 |
| total_pnl | REAL | 总盈亏 |
| total_trades | INTEGER | 总交易数 |
| winning_trades | INTEGER | 盈利交易 |
| losing_trades | INTEGER | 亏损交易 |
| win_rate | REAL | 胜率 |
| total_fees | REAL | 总手续费 |

**✅ 保证：** 每次平仓后自动保存统计快照。

---

## 🚀 使用指南

### 启动系统
```bash
# 启动所有核心服务（交易系统 + XMR监控）
./scripts/start_core_services.sh

# 仅启动交易系统
nohup python3 src/core/integrated_trading_system.py > logs/trading_system.log 2>&1 &
```

### 检查状态
```bash
# 查看服务状态（PID、运行时间、内存占用）
./scripts/check_core_services.sh

# 查看实时日志
tail -f logs/trading_system.log

# 数据库健康检查
python3 src/tools/db_health_check.py
```

### 停止系统
```bash
# 停止所有核心服务
./scripts/stop_core_services.sh

# 重启所有服务
./scripts/restart_core_services.sh
```

---

## 📈 交易流程

### 1. 市场扫描（每5分钟）
系统扫描全部15个币种：
- 获取最新价格和K线数据
- 计算技术指标（MA、RSI、MACD等）
- 生成买卖信号

### 2. 信号执行
当检测到买入信号时：
1. ✅ 检查是否已有持仓（避免重复）
2. ✅ 计算仓位大小（基于2%风险）
3. ✅ 验证风险收益比（≥1:2）
4. ✅ 执行模拟买入
5. ✅ **保存到数据库（trades + positions）**
6. ✅ 发送Telegram通知

当检测到卖出信号时：
1. ✅ 检查是否有持仓
2. ✅ 执行模拟卖出
3. ✅ 计算盈亏（考虑杠杆和手续费）
4. ✅ **保存到数据库（trades + stats）**
5. ✅ 更新持仓状态为closed
6. ✅ 发送Telegram通知（含盈亏详情）

### 3. 风控检查（每30秒）
持续监控所有持仓：
- 🛡️ 价格触及止损 → 自动平仓
- 🎯 价格触及止盈 → 自动平仓
- 💰 计算浮动盈亏
- 📊 更新投资组合状态

### 4. 每日报表（00:00）
自动生成并发送：
- 今日交易记录
- 盈亏统计
- 胜率分析
- 资产变化曲线

---

## 📊 Telegram通知示例

### 开仓通知
```
📈 开仓做多 - BTC/USDT
━━━━━━━━━━━━━━
方向: 做多
价格: $89,621.39
数量: 0.003349
杠杆: 3x
━━━━━━━━━━━━━━
💵 仓位价值: $300.00
💎 保证金: $100.00
💸 手续费: $0.30
━━━━━━━━━━━━━━
🛡️ 止损: $86,932.75
🎯 止盈: $94,998.67
━━━━━━━━━━━━━━
💰 剩余余额: $899.70
```

### 平仓通知
```
📉 平仓 - BTC/USDT
━━━━━━━━━━━━━━
价格: $92,500.00
数量: 0.003349
杠杆: 3x
原因: 止盈
━━━━━━━━━━━━━━
💵 仓位价值: $309.77
💸 手续费: $0.31
🟢 盈亏: +$9.46 (+9.46%)
━━━━━━━━━━━━━━
💰 当前余额: $909.16
📊 总盈亏: +$9.16
💸 总手续费: $0.61
📈 胜率: 100.0%
```

---

## 🔧 数据库保存验证

系统每次交易都会打印验证日志：

### 买入时
```
✅ 交易已保存到数据库 [ID: 123]
✅ 持仓已保存到数据库 [BTC/USDT]
```

### 卖出时
```
✅ 交易已保存到数据库 [ID: 124]
✅ 持仓状态已更新 [BTC/USDT: closed]
✅ 统计已保存到数据库
```

### 错误处理
如果保存失败，会立即显示错误：
```
❌ 保存交易失败: [错误信息]
❌ 保存持仓失败: [错误信息]
```

---

## 🔍 数据库查询示例

### 查看所有交易
```bash
cd /Users/hongtou/newproject/quant-trade-bot
sqlite3 data/db/paper_trading.db "SELECT * FROM trades ORDER BY timestamp DESC LIMIT 10;"
```

### 查看当前持仓
```bash
sqlite3 data/db/paper_trading.db "SELECT * FROM positions WHERE status='open';"
```

### 查看盈亏统计
```bash
sqlite3 data/db/paper_trading.db "SELECT symbol, COUNT(*) as trades, SUM(pnl) as total_pnl FROM trades WHERE side='sell' GROUP BY symbol;"
```

### 查看胜率
```bash
sqlite3 data/db/paper_trading.db "SELECT 
    COUNT(CASE WHEN pnl > 0 THEN 1 END) as wins,
    COUNT(CASE WHEN pnl < 0 THEN 1 END) as losses,
    ROUND(COUNT(CASE WHEN pnl > 0 THEN 1 END) * 100.0 / COUNT(*), 2) as win_rate
FROM trades WHERE side='sell';"
```

---

## ⚙️ 自动化配置

### Crontab自动重启（推荐）
```bash
# 安装crontab任务（每5分钟检查并重启崩溃的服务）
crontab config/crontab_core_services.txt

# 查看已安装的任务
crontab -l
```

### 手动keep-alive
```bash
# 运行保活脚本（检查进程，自动重启）
./scripts/keep_alive.sh
```

---

## 📱 Web监控界面

系统提供Web界面监控：

```bash
# 启动Web监控（端口5001）
./scripts/start_web_monitor.sh

# 访问
http://localhost:5001/

# API接口
curl http://localhost:5001/api/positions  # 当前持仓
curl http://localhost:5001/api/trades     # 交易记录
curl http://localhost:5001/api/stats      # 统计数据
```

---

## 🎯 策略说明

系统使用**多时间框架技术分析策略**：

### 买入条件（需同时满足）
1. ✅ 短期MA上穿长期MA（金叉）
2. ✅ RSI < 70（非超买）
3. ✅ MACD柱状图 > 0（动量向上）
4. ✅ 价格突破布林带中轨
5. ✅ 成交量放大（> 平均值）
6. ✅ 风险收益比 ≥ 1:2

### 卖出条件（任一触发）
1. 🛡️ 价格触及止损价（-3%）
2. 🎯 价格触及止盈价（+6%）
3. 📉 短期MA下穿长期MA（死叉）
4. 📉 RSI > 80（严重超买）
5. 📉 MACD柱状图 < 0（动量转弱）

---

## 💡 使用建议

### 🟢 优势
- 完全自动化，24/7运行
- 多币种分散风险
- 严格风控，限制单笔亏损
- 完整记录，便于复盘
- 实时通知，掌握动态

### ⚠️ 注意事项
1. **模拟交易**：这是模拟系统，使用虚拟资金
2. **网络依赖**：需稳定网络连接Binance API
3. **资金管理**：建议初始资金不超过$10,000
4. **监控日志**：定期检查logs目录
5. **数据备份**：定期备份data/db/目录

### 📈 优化方向
- 添加更多技术指标（KDJ、威廉指标等）
- 实现动态止损（移动止损、追踪止损）
- 引入机器学习预测
- 增加回测功能验证策略
- 支持更多交易所（OKX、Bybit等）

---

## 📞 问题排查

### 服务不运行
```bash
# 检查进程
ps aux | grep "integrated_trading_system"

# 查看错误日志
tail -100 logs/trading_system.log

# 重启服务
./scripts/restart_core_services.sh
```

### 没有交易信号
- 检查网络连接
- 验证币安API可访问
- 查看日志中的扫描记录
- 可能当前市场无符合条件的信号

### Telegram通知失败
- 检查config/config.json中的bot_token和chat_id
- 验证Telegram bot是否添加到群组
- 查看日志中的通知发送记录

### 数据库问题
```bash
# 运行健康检查
python3 src/tools/db_health_check.py

# 检查文件权限
ls -la data/db/paper_trading.db

# 备份数据库
cp data/db/paper_trading.db data/db/paper_trading_backup_$(date +%Y%m%d).db
```

---

## 📚 相关文档

- [核心服务管理指南](CORE_SERVICES_GUIDE.md)
- [项目结构说明](../README.md)
- [文件重组计划](FILE_REORGANIZATION_PLAN.md)

---

## 📊 系统状态
- ✅ 实盘模拟交易：运行中
- ✅ XMR定时更新：运行中
- ✅ 数据库：健康
- ✅ Telegram通知：已启用
- ✅ 监控币种：15个
- ✅ 数据保存：100%完整

**最后更新**: 2026-01-23 19:40
